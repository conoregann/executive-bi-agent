# Semantic metrics

Metrics are product contracts, not prompt text. Each metric should define its grain, time semantics, filters, dimensions, calculation, freshness, owner, and validation cases before it is exposed to the agent.
